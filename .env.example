# Server Configuration
PORT=5000
NODE_ENV=production

# ═══════════════════════════════════════════════════════
# Email Configuration — Choose ONE method:
# ═══════════════════════════════════════════════════════

# OPTION 1: Resend (RECOMMENDED for Render / cloud hosting)
# Sign up free at https://resend.com → Get API key
# Free tier: 100 emails/day, 3000/month
# Works on ALL hosting platforms (uses HTTPS, not SMTP)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM=onboarding@resend.dev

# OPTION 2: Gmail SMTP (works locally, may timeout on some hosts)
# Go to https://myaccount.google.com/apppasswords
# Generate an App Password for "Mail"
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=mindcraftjpr@gmail.com
EMAIL_PASS=your-16-digit-app-password

# Recipient email for all notifications
EMAIL_TO=mindcraftjpr@gmail.com

# Admin key to view form submissions at /api/contact/submissions
# Set any random string (e.g. use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ADMIN_KEY=change-this-to-a-random-string
